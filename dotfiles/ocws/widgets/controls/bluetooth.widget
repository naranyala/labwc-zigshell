#Api2

# Bluetooth text widget — shows BT status
export button "bluetooth" {
  interval = 3000
  style = "text_widget"
  tooltip = "Bluetooth: " + XBtState
  action = Exec("bluetoothctl show | grep Powered")

  label {
    value = If(XBtState = "On", "󰂯 BT", "󰂲 BT Off")
  }
}

#CSS
