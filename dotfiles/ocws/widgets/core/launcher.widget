#Api2

# Launcher widget — text-style app menu
export button "launcher" {
  style = "text_widget"
  class = "module"
  tooltip = "Application Launcher"
  action = Exec("rofi -show drun")

  label { value = "  Apps" }
}

#CSS
