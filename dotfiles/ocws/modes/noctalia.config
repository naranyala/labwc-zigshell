#Api2
# modes/noctalia.config — Noctalia/DankMaterialShell Dynamic Island Bar
# Relies on the C-patched sfwbar for floating island geometry

bar "topbar:top" {
  edge = "top"
  layer = "top"
  mirror = "*"
  
  # The C patch natively handles making the GTK window full-span and transparent.
  # halign = "center" applies to the inner #ebox!
  halign = "center"
  valign = "start"

  # Dynamic Island Content
  widget "workspaces"
  widget "sysmon"
  widget "clock"
}
